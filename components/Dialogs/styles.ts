import { styled } from 'styled-components'

import { RoundButton } from '@/styles/general'

export const Pane = styled.div`
  margin: 0;
`
export const StarBox = styled(RoundButton)`
  position: absolute;
  top: 1rem;
  right: 1rem;
`
